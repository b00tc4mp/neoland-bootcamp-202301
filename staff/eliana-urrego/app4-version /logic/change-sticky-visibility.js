// function changeStickyVisibility(id, user, visibility)
// var user = users.find(user => user.email === email)

// //case 1

// if (!user) throw new Error('user with email ' + email + ' not found')

// if(sticky.visibility === visibility.private)